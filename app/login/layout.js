import "../globals.css";

export const metadata = {
  title: "Login | SpringHouse",
  description: "SpringHouse Admin Login",
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
